pipeline {
    agent any
    stages {
        stage('Clone') {
            steps{
                git branch: 'main', url: 'https://github.com/halephu01/Jenkins-CI-CD.git'
            }
        }
    }
}